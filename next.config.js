/** @type {import('next').NextConfig} */
const nextConfig = {}

module.exports = nextConfig



// in js next  config file we keep next js  configuration  like env , we can even define about function length ...